class Client

types
		-- a type that represents a String, by using a sequence of char
		public String = seq of char;

instance variables
		-- a client is identified by his name
		private name : String;

operations
		public Client: String ==> Client
		Client(string) == (
			name := string;
			return self);

end Client